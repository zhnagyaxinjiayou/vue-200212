export const getters = {
         isFlor(state) {
           return state.isFlor;
         },
         ifLoding(state) {
           return state.ifLoding;
         },
         errMag(state) {
           return state.errMag;
         },
         users(state) {
           return state.users;
         },
       };